<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/service_nav"
    app:startDestination="@id/navigation_choose_service">
    <fragment
        android:id="@+id/navigation_choose_service"
        android:name="com.enugu.pension.ui.fragment.service.ChooseServiceFragment"
        android:label="Choose Service"
        tools:layout="@layout/fragment_choose_service">
        <action
            android:id="@+id/action_chooseservice_to_ActiveService"
            app:destination="@id/navigation_activeService"
             />
        <action
            android:id="@+id/action_chooseservice_to_Retiree"
            app:destination="@id/navigation_Retiree"
           />
    </fragment>
    <fragment
        android:id="@+id/navigation_activeService"
        android:name="com.enugu.pension.ui.fragment.service.ActiveServiceFragment"
        android:label="Active Service"
        tools:layout="@layout/fragment_active_service">
        <action
            android:id="@+id/action_navigation_activeService_to_swiftVerificationDialog"
            app:destination="@id/swiftVerificationDialog" />
    </fragment>
    <fragment
        android:id="@+id/navigation_Retiree"
        android:name="com.enugu.pension.ui.fragment.service.RetireeServiceFragment"
        android:label="Retiree Service"
        tools:layout="@layout/fragment_retiree">
        <action
            android:id="@+id/action_navigation_Retiree_to_swiftVerificationDialog"
            app:destination="@id/swiftVerificationDialog" />
    </fragment>
    <dialog
        android:id="@+id/swiftVerificationDialog"
        android:name="com.enugu.pension.ui.dialog.SwiftVerificationDialog"
        android:label="SwiftVerificationDialog"
        tools:layout="@layout/dialog_swift_confirmation"/>
</navigation>